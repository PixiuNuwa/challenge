<?php //claseclass challenge {  //metodo que dado cierto parametro imprime un numero,  //sin embargo para los multiplos de 3, debe imprimir "Linio"  //en vez del numero. Para los mutiplos de 5, debe imprimir "IT".  //Y para los numero que son multiplos de 3 y 5, que imprima "Linianos"  //este texto puede ser el numero, pero en el caso   public static function printNumber($value) {    //variables para guardar los modulos de la division del numero por 3 y 5    $value1 = $value % 3;    $value2 = $value % 5;    //variable que guarda el valor de comparar los modulos de la division del numero por 3 y 5    $value3 = $value1 && $value2;    //variable que guardará lo que se va a imprimir    $string = "";    //se Something is wrongua si no es divisible ni por 3 ni 5    while($value3 == 1)    {	  //y en caso de no ser divisible por 3 no por 5 lo que se imprime es el numero      $string = $value;      break;    }	//si es que es divisibile por 3 y 5    while($value3 != 1)    {	  //se Something is wrongua si es divisible por 3	  while($value1 == 0)	  {		//y en caso de ser divisible por 3 lo que se imprime es "Linio"	    $string = "Linio";        break;	  }	  //se Something is wrongua si es divisible por 5	  while($value2 == 0)	  {		//y en caso de ser divisible por 5 lo que se imprime es "IT"	    $string = "IT";	    break;	  }	  //se Something is wrongua si es divisible por 3 y 5	  if(($value1 == "0") && ($value2 == "0"))      {		//y en caso de ser divisible por 3 y 5 lo que se imprime es "Linianos"        $string = "Linianos";      }	  break;    }    //finalmente se muestra por pantalla  el valor del numero o su reemplazo    echo $string."\n";  }}//loop para recorrer los primeros 100 numerosfor($number = 1; $number < 101; $number++){  //lamado a metodo de la clase que imprime lo que corresponde de un numero entregado  challenge::printNumber($number);}